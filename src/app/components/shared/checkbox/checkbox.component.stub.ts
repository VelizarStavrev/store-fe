import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-checkbox',
  template: '',
})
export class CheckboxComponentStub {
  @Input() label: any;
  @Input() checked: any;
}
