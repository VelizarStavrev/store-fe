export interface ButtonOptions {
  buttonText?: string,
  buttonType?: 'primary' | 'secondary' | 'transparent',
  buttonHTMLType?: 'button' | 'submit',
  buttonMarginTop?: boolean,
  buttonMarginBottom?: boolean,
  buttonMarginLeft?: boolean,
  buttonMarginRight?: boolean,
  buttonDisabled?: boolean,
  buttonWidthFull?: boolean,
}
